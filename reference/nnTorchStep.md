# Optimizer step; also syncs updated weights into the loader buffer

Optimizer step; also syncs updated weights into the loader buffer

## Usage

``` r
nnTorchStep(id)
```

## Arguments

- id:

  network id.

## Value

invisibly NULL.
