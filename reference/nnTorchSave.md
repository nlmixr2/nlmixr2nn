# Save / load a trained torch module to a binary

Save / load a trained torch module to a binary

## Usage

``` r
nnTorchSave(id, file)

nnTorchLoad(id, file)
```

## Arguments

- id:

  network id.

- file:

  path to the serialized module.

## Value

invisibly NULL.
