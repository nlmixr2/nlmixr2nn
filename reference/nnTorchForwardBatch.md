# Batch forward pass of a torch module

Batch forward pass of a torch module

## Usage

``` r
nnTorchForwardBatch(id, X)
```

## Arguments

- id:

  network id.

- X:

  numeric matrix of inputs, \`N\` rows by \`K\` columns.

## Value

numeric vector of length \`N\`.
