# Push a torch module's weights into the loader buffer (C path, no R round-trip)

Push a torch module's weights into the loader buffer (C path, no R
round-trip)

## Usage

``` r
nnTorchSync(id)
```

## Arguments

- id:

  network id.

## Value

invisibly, the number of weights synced.
