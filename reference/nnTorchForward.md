# Forward pass of a torch module at one input (for validation)

Forward pass of a torch module at one input (for validation)

## Usage

``` r
nnTorchForward(id, x)
```

## Arguments

- id:

  network id.

- x:

  numeric input vector of length K.

## Value

numeric scalar.
