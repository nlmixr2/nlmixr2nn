# Flat weights of a torch module (nnWeightLayout order)

Flat weights of a torch module (nnWeightLayout order)

## Usage

``` r
nnTorchWeights(id)
```

## Arguments

- id:

  network id.

## Value

numeric vector.
