% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nnTrain.R
\name{nnTrain}
\alias{nnTrain}
\title{Fit an embedded neural network in an ODE by forward-sensitivity + torch}
\usage{
nnTrain(
  model,
  data,
  pred,
  nnId = 0L,
  K,
  H,
  act = "tanh",
  params = numeric(0),
  inits = numeric(0),
  optimizer = "adam",
  lr = 0.05,
  iter = 50,
  sigma = 1,
  estSigma = TRUE,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{model}{rxode2 model text with one `g = nn<K>(id, ...)` output and the
weight block declared via `param(nnWeightLayout(...))`.}

\item{data}{event data frame (columns id, time, evid, dv, amt as needed).}

\item{pred}{name of the predicted state that `dv` observes.}

\item{nnId, K, H, act}{network id, input dim, hidden width, activation.}

\item{params}{named numeric of fixed population parameters (non-weight).}

\item{inits}{named numeric of base-state initial conditions.}

\item{optimizer}{"adam" or "sgd"; `lr` learning rate; `iter` iterations.}

\item{sigma}{initial residual SD; `estSigma` profiles it by MLE each iteration.}

\item{seed}{optional torch init seed; `verbose` prints the LL trace.}
}
\value{
list(weights, sigma, ll, llTrace).
}
\description{
Fit an embedded neural network in an ODE by forward-sensitivity + torch
}
\keyword{internal}
