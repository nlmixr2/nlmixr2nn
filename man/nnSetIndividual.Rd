% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nnEval.R
\name{nnSetIndividual}
\alias{nnSetIndividual}
\title{Mark a network as individual (pop = FALSE): per-subject weights via weight etas}
\usage{
nnSetIndividual(id, etaBase, etaModel = c("prop", "add"))
}
\arguments{
\item{id}{network id.}

\item{etaBase}{0-based index of the first weight-eta in the model's eta vector.}

\item{etaModel}{"prop" (`W = lW * exp(etaW)`) or "add" (`W = lW + etaW`).}
}
\value{
invisibly TRUE.
}
\description{
The network's weights become individual, `W_i = f(lW, etaW)`, where `lW` are
the population (torch) weights and `etaW` are `nW` random effects starting at
`etaBase` in the model's eta vector.  FOCEI's inner weight-injection hook
applies this each time a subject's etas are set.
}
