% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nnUdf.R
\name{nn}
\alias{nn}
\title{Neural-network term for an rxode2/nlmixr2 model}
\usage{
nn(
  ...,
  n_hidden = 5L,
  act = c("relu", "softplus", "tanh", "gelu", "silu"),
  sd = 0.1,
  num = NULL,
  iniDf = NULL
)
}
\arguments{
\item{...}{one or more state/covariate inputs to the network (given
positionally, e.g. `nn(central, t)`).}

\item{n_hidden}{hidden-layer width (default 5).}

\item{act}{activation, one of "relu", "softplus", "tanh".}

\item{sd}{standard deviation of the random weight initialization.}

\item{num}{network occurrence number; queried via `rxUdfUiNum()` if `NULL`.}

\item{iniDf}{initial-estimate data.frame; queried via `rxUdfUiIniDf()` if
`NULL`.}
}
\value{
a list consumed by [rxode2::rxUdfUi()] (`replace`, `before`, `iniDf`).
}
\description{
Use `nn(state, ..., n_hidden=, act=)` inside a model to insert a
single-hidden-layer neural network of the state input(s).  At parse time it
is replaced by a compiled `nn<K>()` call and its weights are added to the
model as randomly-initialized population parameters.
}
