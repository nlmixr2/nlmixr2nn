% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nnControl.R
\name{nnControl}
\alias{nnControl}
\title{Control for the neural-network estimation method (`est = "nn"`)}
\usage{
nnControl(
  inner = nlmixr2est::foceiControl(),
  rounds = 15L,
  wSteps = 8L,
  lr = 0.03,
  optimizer = c("adam", "sgd"),
  seed = NULL,
  mode = c("alternating")
)
}
\arguments{
\item{inner}{an inner estimation control object (default
`nlmixr2est::foceiControl()`).  Its class names the inner estimator.}

\item{rounds}{number of alternating rounds (inner fit + weight updates).}

\item{wSteps}{torch weight-optimizer steps taken per round.}

\item{lr}{torch optimizer learning rate.}

\item{optimizer}{torch optimizer, `"adam"` or `"sgd"`.}

\item{seed}{optional integer seed for the torch weight initialization.}

\item{mode}{training mode; currently only `"alternating"`.}
}
\value{
an object of class `c("nnControl", class(inner))` carrying the NN
  schedule in its `"nnControl"` attribute.
}
\description{
Wraps an inner NLME control (e.g. [nlmixr2est::foceiControl()]) and adds the
neural-network training schedule.  The returned object extends the inner
control's class, so the inner estimator is inferred from it and the alternating
loop drives that estimator each round.
}
\author{
Matthew L. Fidler
}
