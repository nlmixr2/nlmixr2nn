% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nnJoint.R
\name{nnControl}
\alias{nnControl}
\title{Control for the joint neural-network estimation method (`est = "nn"`)}
\usage{
nnControl(
  inner = nlmixr2est::foceiControl(),
  maxRounds = 200L,
  outerPerRound = 1L,
  wSteps = 2L,
  tol = 0.001,
  lr = 0.03,
  warmSteps = 0L,
  optimizer = c("adam", "sgd"),
  seed = NULL
)
}
\arguments{
\item{inner}{an inner estimation control object (default
`nlmixr2est::foceiControl()`).  Its class names the inner estimator.}

\item{maxRounds}{MAXIMUM number of interleaved rounds.}

\item{outerPerRound}{inner outer-iterations taken per round (small, e.g. 1--3);
the partial step that makes this joint rather than a full re-fit.}

\item{wSteps}{torch weight-optimizer steps per round.}

\item{tol}{convergence tolerance on the relative round-to-round objective and
weight change (both must be below it to stop).}

\item{lr}{torch optimizer learning rate.}

\item{warmSteps}{optional naive-pooled warm-up steps before the loop (default 0).}

\item{optimizer}{torch optimizer, `"adam"` or `"sgd"`.}

\item{seed}{optional integer seed for the torch weight initialization.}
}
\value{
an object of class `c("nnControl", class(inner))` carrying the joint
  schedule in its `"nnControl"` attribute.
}
\description{
Wraps an inner NLME control (e.g. [nlmixr2est::foceiControl()]) for the *joint*
method `est = "nn"`, which co-optimizes the population parameters and the NN
weights in a single interleaved loop (contrast [nnIterControl()], which runs a
full inner fit each round).  The returned object extends the inner control's
class, so the inner estimator is inferred from it.
}
\details{
**Stopping.**  Each round takes `outerPerRound` inner outer-iterations
(warm-started from the previous round) plus `wSteps` torch weight steps.  The
loop stops when both the relative objective change and the relative weight
change fall below `tol`, or after `maxRounds`.  `$nnConverged`, `$nnRounds` and
`$nnParHist` (with `objfChange`/`wChange` columns) report the outcome.
}
\author{
Matthew L. Fidler
}
